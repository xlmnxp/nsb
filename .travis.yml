language: node_js
sudo: true
node_js: "stable"
install:
  - npm install
script:
  - npm start

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master